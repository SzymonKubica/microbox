/**
 * A proper % operator that returns a positive remainder even if the input is
 * negative.
 */
int mathematical_modulo(int a, int b);
int modulo_increment(int a, int b);
int modulo_decrement(int a, int b);
